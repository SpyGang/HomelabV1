services:

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./conf.d:/etc/nginx/conf.d
    mem_limit: 64m
    networks:
      - labnet

networks:
  labnet:
    external: true
